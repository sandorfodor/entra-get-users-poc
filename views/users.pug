doctype html

html(lang='en')
  head
    style.
      table {
        width: 80%;
      }
  body
    h3 Users in tenant
    table.table.table-striped
      tr
        th ID
        th Name
        th Email

      each u in users
        tr
          td= u.id
          td= u.displayName
          td= u.mail